# Production Configuration
spring.profiles.active=prod

# Database Configuration for Production (example with PostgreSQL)
# spring.datasource.url=jdbc:postgresql://localhost:5432/mcp_test_db
# spring.datasource.username=mcp_user
# spring.datasource.password=secure_password
# spring.datasource.driver-class-name=org.postgresql.Driver
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.hibernate.ddl-auto=validate

# Logging for Production
logging.level.com.mcp.testserver=INFO
logging.level.org.springframework.web=WARN
logging.level.org.hibernate.SQL=WARN
logging.file.name=logs/mcp-test-server.log
logging.logback.rollingpolicy.max-file-size=50MB
logging.logback.rollingpolicy.max-history=30

# Security for Production
server.ssl.enabled=true
# server.ssl.key-store=classpath:keystore.p12
# server.ssl.key-store-password=changeit
# server.ssl.key-store-type=PKCS12
# server.ssl.key-alias=mcp-test-server

# Performance Configuration
server.tomcat.max-threads=200
server.tomcat.accept-count=100
server.tomcat.connection-timeout=20000
